// string
let greeting: string = "Hey, Allan";
console.log(greeting);

// number
let userAge: number = 22
let userId: number = 32145.2
console.log(userAge, userId);

// boolean
let isLoggedIn: boolean = true; 
console.log(isLoggedIn);

// type inference
// tyepscript is smart enough to tell the type of variable even without implicitly assigning the type eg. let age = 22; means its a number itself


// any
// do not use 'any'. in simple words, it means turning off type checking

export {}


